apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-webhook.fullname" . }}
  labels:
    {{- include "alert-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alert-webhook.selectorLabels" . | nindent 4 }}
