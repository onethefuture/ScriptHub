apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alert-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "alert-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alert-webhook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
